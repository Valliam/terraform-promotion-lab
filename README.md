# Terraform Promotion Lab

Hands-on lab for learning a production-style Terraform flow:

`feature branch -> pull request -> terraform plan -> merge -> apply dev -> promote staging -> promote production`
